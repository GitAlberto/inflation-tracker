[pytest]
# Ajoute la racine et src/ au sys.path pour les imports api.*, model.*, collect.*, aggregate.*
pythonpath = . src

# Suppression des warnings internes aux librairies tierces (matplotlib/pyparsing)
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
